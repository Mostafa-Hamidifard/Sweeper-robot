// #include <Arduino.h>
// class TaskScheduler {
// public:
//   void addTask(void (*task)(), unsigned long interval);
//   void runTasks();

// private:
//   struct Task {
//     void (*taskFunc)();      // Pointer to a function that runs the task
//     unsigned long interval;  // Interval between task executions in milliseconds
//     unsigned long lastRun;   // Time when the task was last run
//   } tasks[10];               // Array to hold up to 10 tasks, adjust size as needed
//   int numTasks = 0;  // Number of tasks currently registered
// };
